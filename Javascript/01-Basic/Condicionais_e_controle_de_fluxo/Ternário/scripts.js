// Operador condicional ternário

let age = 16;
console.log(
  age >= 18 //Condição
    ? "Você pode dirigir" // Se verdadeiro
    : "Você não pode dirigir"
); // Se falso

console.log(age < 18 ? "Você não pode dirigir" : "Você pode dirigir");
