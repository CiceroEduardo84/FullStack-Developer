// defaukt export - é a função padrão fornecida pelo módulo.
export default function soma(a, b) {
  return a + b;
}

// Named export - cada método é importado pelo seu próprio nome de exportação.
export function multiplicacao(a, b) {
  return a * b;
}
